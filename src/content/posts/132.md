---
title: "vue在elementUI el-col元素上绑定@click事件无效解决办法"
date: "2022-03-05T09:57:00.000Z"
categories:
  - "JavaScript"
tags:
  - "vue"
description: "在el-col直接添加 `@click=\"themeHandleClose(item)\"`， 点击后无效没有反馈事件并没有执行 ``` 设置主题 @click.native=\"themeHandleClose(item)\" class=\"c ..."
typechoCid: 132
typechoSlug: "132"
draft: false
---

在el-col直接添加 `@click="themeHandleClose(item)"`， 点击后无效没有反馈事件并没有执行
```
<el-drawer
    :visible.sync="drawer"
    :show-close="true"
    :with-header="false">
    <el-row
        id="config-panel"
        :gutter="20">
        <el-col :span="24">设置主题</el-col>
        <el-col
            :span="8"
            :key="key"
            v-for="(item, key) in themeList"
            <!--@click="themeHandleClose(item)"-->
            @click.native="themeHandleClose(item)"
            class="config-panel-item">
            <div class="grid-content">{{item}}</div>
        </el-col>
    </el-row>
</el-drawer>
```

这时我们将`@click`替换为`@click.native`即可
